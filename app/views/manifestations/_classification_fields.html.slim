    = f.select(:classification_type_id, @classification_types.collect{|c| [c.display_name.localize, c.id]})
    = f.text_field :category
    = link_to_remove_association t('page.remove'), f, confirm: t('page.are_you_sure')

